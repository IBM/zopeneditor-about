import{c as e}from"./app.087f62ff.js";import{_ as s}from"./plugin-vue_export-helper.21dcd24c.js";const n={},r=e(`<h1 id="interact-with-rse-common-properties" tabindex="-1"><a class="header-anchor" href="#interact-with-rse-common-properties" aria-hidden="true">#</a> Interact with RSE Common Properties</h1><p>The Common Properties service is an optional feature available in IBM z/OS Explorer and must be configured and enabled in the host configuration file (<code>rse.env</code>).</p><p>To enable the Common Properties service, a system programmer must set the <code>RSE_COMMON_PROPERTIES</code> variable in the <code>rse.env</code> file. For example, <code>RSE_COMMON_PROPERTIES=/usr/shared/common.properties</code>.</p><p>Common Properties provides services for users to store JSON objects in different namespaces. The namespace can be globally accessible by all users or only accessible by the creator of the namespace. Namespace is implemented internally as a JSON document. You can interact with the Common Properties service by using commands that are provided by the IBM\xAE RSE API plug-in for Zowe\u2122 CLI (RSE CLI plug-in) in the Terminal window.</p><h2 id="using-rse-cli-plug-in-commands" tabindex="-1"><a class="header-anchor" href="#using-rse-cli-plug-in-commands" aria-hidden="true">#</a> Using RSE CLI plug-in commands</h2><p>RSE API Plug-in for Zowe CLI allows users to store JSON configuration or any key-value properties in a namespace that can be shared globally with all users. The namespace can also be created as nonsharable and only accessible by the creator.</p><p>Here is a use case for storing and retrieving server configuration properties by using the RSE Common Properties service. First, create a global namespace to be shared for all users. If you need to create a private namespace without sharing, you need to include the <code>--private</code> option in the commands.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>% zowe rse common-properties create ns1
Namespace created successfully.
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Next, save the test server configuration with hostname and port number as one JSON object at the path of /test-server in the namespace.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>$ zowe rse common-properties set ns1 /test-server &#39;{&quot;hostname&quot; : &quot;www.test.com&quot;, &quot;port&quot; : 1234}&#39; --type json
Property set successfully.
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>You can also save server configuration properties individually. The supported types of the property value are simple string, JSON string, number, and boolean.</p><p>To save the production server configuration properties, run the following command:</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>$ zowe rse common-properties set ns1 /prod-server/hostname &quot;www.prod.com&quot; 
Property set successfully.
$ zowe rse common-properties set ns1 /prod-server/port 443 --type number
Property set successfully.
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>When setting a property in the namespace, the namespace will be created if it doesn&#39;t exist yet. If the property already exists, its value will be overwritten.</p><p>To view the namespace content, run the following command:</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>$ zowe rse common-properties view ns1
{
  &quot;test-server&quot;: {
    &quot;hostname&quot;: &quot;www.test.com&quot;,
    &quot;port&quot;: 1234
  },
  &quot;prod-server&quot;: {
    &quot;hostname&quot;: &quot;www.prod.com&quot;,
    &quot;port&quot;: 443
  }
}
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><p>To view the production server configuration properties, run the following command:</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>% zowe rse common-properties view ns1 --path /prod-server
{
  &quot;hostname&quot;: &quot;www.prod.com&quot;,
  &quot;port&quot;: 443
}
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>To view the production server hostname value, run the following command:</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>% zowe rse common-properties view ns1 --path /prod-server/hostname
www.prod.com
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>To delete a property from the namespace, run the following command:</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>% zowe rse common-properties delete ns1 --path /prod-server
Property deleted successfully.
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>There are other commands to list, copy, rename, and delete namespace. You can check common-properties command help for a complete list of common-properties commands.</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>% zowe rse common-properties --help
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div>`,24);function a(o,t){return r}var c=s(n,[["render",a]]);export{c as default};
