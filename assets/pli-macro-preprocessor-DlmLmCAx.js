import{a as e,c as t,i as n,l as r,n as i,o as a,r as o,s,t as c}from"./app-B5Q-czM2.js";var l=JSON.parse(`{"path":"/Blog/pli-macro-preprocessor.html","title":"No More Asking a Robot to Read Your Code for You: Native PL/I Macro Support in Z Open Editor 6.7.0","lang":"en-US","frontmatter":{},"git":{"updatedTime":1790291987000,"contributors":[{"name":"RUSS MAY","username":"","email":"Russell.May@ibm.com","commits":1},{"name":"Esther M","username":"","email":"Esther.M@ibm.com","commits":1},{"name":"phaumer","username":"phaumer","email":"4391934+phaumer@users.noreply.github.com","commits":1,"url":"https://github.com/phaumer"}],"changelog":[{"hash":"307783f67f36f7d582723de86a6707257b0e4bc8","time":1790291987000,"email":"Russell.May@ibm.com","author":"RUSS MAY","message":"pli-macro-support-blog (#658)","coAuthors":[{"name":"Esther M","email":"Esther.M@ibm.com"},{"name":"Peter Haumer","email":"4391934+phaumer@users.noreply.github.com"}]}]},"filePathRelative":"Blog/pli-macro-preprocessor.md"}`),u={name:`pli-macro-preprocessor.md`};function d(c,l,u,d,f,p){let m=t(`RouteLink`);return s(),o(`div`,null,[l[3]||=n(`<h1 id="no-more-asking-a-robot-to-read-your-code-for-you-native-pl-i-macro-support-in-z-open-editor-6-7-0" tabindex="-1"><a class="header-anchor" href="#no-more-asking-a-robot-to-read-your-code-for-you-native-pl-i-macro-support-in-z-open-editor-6-7-0"><span>No More Asking a Robot to Read Your Code for You: Native PL/I Macro Support in Z Open Editor 6.7.0</span></a></h1><p>If you write PL/I for IBM Z®, you have probably stared at your editor and thought: <em>&quot;Why is it yelling at me? This code compiles fine.&quot;</em></p><p>The answer, almost certainly, is macros.</p><h2 id="the-old-approach-a-k-a-please-hold-while-we-outsource-your-parsing" tabindex="-1"><a class="header-anchor" href="#the-old-approach-a-k-a-please-hold-while-we-outsource-your-parsing"><span>The old approach (a.k.a. &quot;please hold while we outsource your parsing&quot;)</span></a></h2><p>PL/I&#39;s macro preprocessor is powerful. Before the compiler sees your code, a preprocessing step evaluates <code>%IF</code>/<code>%THEN</code>/<code>%ELSE</code> branches, expands <code>%DECLARE</code>d variables, calls <code>%PROCEDURE</code>s, and produces a clean stream of plain PL/I for the compiler to process.</p>`,5),i(`p`,null,[l[1]||=e(`The editor, however, was not the compiler. It saw what you typed, raw macros and all, and had to make sense of it. The workaround has long been a `,-1),a(m,{to:`/Blog/announcing-preprocessor.html#how-do-you-extend-the-use-of-preprocessor-for-expanding-pl-i-macros`},{default:r(()=>[...l[0]||=[e(`configurable external preprocessor`,-1)]]),_:1}),l[2]||=e(`: point the editor to a local binary or a remote preprocessor running on z/OS®, and it runs your source through the preprocessor, compares the result with the original source, and:`,-1)]),l[4]||=n(`<ul><li><strong>Color the original source</strong> to show which tokens change after preprocessing</li><li><strong>Shows the preprocessed text in a hover</strong> on those highlighted tokens</li><li><strong>Report errors only against the preprocessed output</strong>, so squiggly red lines reflect real compiler errors rather than complaints about unexpanded directives</li></ul><p>This approach still works, and it remains available. However, it has a limitation that frustrated many users: macro declarations and procedure definitions all received the same flat color, and hovering over them showed what a token expanded <em>to</em> — but not <em>why</em>. The preprocessor expands them away entirely, so the diff can only indicate that they are gone. You could see the output, but not the meaning: not what type a variable was, not what value it currently held, and not where a procedure was declared.</p><h2 id="what-changed-in-6-7-0" tabindex="-1"><a class="header-anchor" href="#what-changed-in-6-7-0"><span>What changed in 6.7.0</span></a></h2><p>We built a native macro preprocessor directly into the editor. No external binary, remote connection, or configuration is required. Open a <code>.pli</code> file and the editor understands your macros right out of the box.</p><p>More than simply eliminating setup friction, the native preprocessor provides a significantly better editing experience:</p><p><strong>Semantically-aware syntax coloring.</strong> Macro variables, macro procedure declarations, and regular PL/I code are each colored distinctly and correctly because the editor now <em>understands</em> what each token is, not just that it differs from external output.</p><p><strong>Rich, live hover information.</strong> Hover over a <code>%DECLARE</code>d variable and see its type and current value at that point in the file. Hover over a macro procedure and see the procedure signature. The information reflects the actual macro state rather than a diff artifact.</p><p><strong>Go to Declaration for macros.</strong> You can now navigate to the declaration of a macro variable or procedure in the same as any other PL/I symbol. Click through, jump back. It works exactly as expected.</p><p><strong>Errors that make sense.</strong> Invalid or incorrect macro directives are flagged directly where they appear, and errors in the resulting code are reported against the post-preprocessed view so that every diagnostic reflects an actual issue.</p><h2 id="a-quick-example" tabindex="-1"><a class="header-anchor" href="#a-quick-example"><span>A quick example</span></a></h2><p>Here is the classic &quot;ZIP code lookup&quot; example from the PL/I Programming Guide, a source file that uses <code>%IF</code>/<code>%ELSE</code> to emit either a function or a subroutine depending on a macro variable:</p><div class="language-pli line-numbers-mode" data-highlighter="prismjs" data-ext="pli"><pre><code class="language-pli"><span class="line"> %DCL USE CHAR;</span>
<span class="line"> %USE = &#39;SUB&#39;;   /* &#39;FUN&#39; for a function, &#39;SUB&#39; for a subroutine */</span>
<span class="line"></span>
<span class="line"> %IF USE = &#39;FUN&#39; %THEN %DO;</span>
<span class="line">   CITYFUN: PROC(ZIPIN) RETURNS(CHAR(16)) REORDER;</span>
<span class="line"> %END;</span>
<span class="line"> %ELSE %DO;</span>
<span class="line">   CITYSUB: PROC(ZIPIN, CITYOUT) REORDER;</span>
<span class="line">   DCL CITYOUT CHAR(16);</span>
<span class="line"> %END;</span>
<span class="line"></span>
<span class="line">   DCL ZIPIN PIC &#39;99999&#39;;</span>
<span class="line">  ...</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Without a preprocessor configured, the editor sees <em>both</em> procedure declarations simultaneously and flags them as conflicting errors because, from the raw parser&#39;s point of view, both branches appear to be active at the same time.</p><p>With the external preprocessor configured, those errors disappear because the editor receives the post-processed output and knows which branch was taken. Hovering over a highlighted token shows what it expanded <em>to</em>, but not <em>why</em>. <code>%USE</code>, <code>%DCL</code>, and the <code>CITYSUB:</code> label all receive the same flat coloring, and because the preprocessor expands them away entirely, the diff can only indicate that they are gone. It cannot tell the editor what they represent, what type <code>USE</code> is, or what its current value is.</p><p>With the native preprocessor, the editor evaluates <code>%USE = &#39;SUB&#39;</code>, takes the correct branch, and shows you only <code>CITYSUB</code>, with proper semantic coloring for every macro token, a hover on <code>USE</code> that shows its type and current value <code>&#39;SUB&#39;</code>, and Go to Declaration support for both the variable and the procedure. No false errors appear, and no context is lost.</p><p><img src="/zopeneditor-about/assets/pl1-macro-hover-Ciucs2Zv.png" alt="PLI macro hover"></p><h2 id="what-the-native-preprocessor-handles" tabindex="-1"><a class="header-anchor" href="#what-the-native-preprocessor-handles"><span>What the native preprocessor handles</span></a></h2><p>The implementation supports the full standard PL/I macro language:</p><table><thead><tr><th>Category</th><th>What&#39;s supported</th></tr></thead><tbody><tr><td>Variable declarations</td><td><code>%DECLARE</code> / <code>%DCL</code> — scalar and array <code>CHAR</code> and <code>FIXED</code> variables</td></tr><tr><td>Assignment</td><td><code>%var = expr;</code> with full expression evaluation</td></tr><tr><td>Conditionals</td><td><code>%IF</code> / <code>%THEN</code> / <code>%ELSE</code> / <code>%DO</code> / <code>%END</code></td></tr><tr><td>Loops</td><td><code>%DO</code>...<code>%END</code> with <code>%ITERATE</code> and <code>%LEAVE</code></td></tr><tr><td>Control flow</td><td><code>%GOTO</code></td></tr><tr><td>Macro procedures</td><td><code>%label: PROC(...) [RETURNS(type)];</code> with <code>ANSWER</code>/<code>ANS</code> and <code>RETURN</code></td></tr><tr><td>Activation</td><td><code>%ACTIVATE</code> / <code>%DEACTIVATE</code> with <code>SCAN</code>, <code>RESCAN</code>, <code>NORESCAN</code></td></tr><tr><td>Built-in functions</td><td>All 30+ standard preprocessor built-ins: <code>COLLATE</code>, <code>COPY</code>, <code>COUNTER</code>, <code>DIMENSION</code>, <code>HBOUND</code>, <code>LBOUND</code>, <code>INDEX</code>, <code>LENGTH</code>, <code>LOWERCASE</code>, <code>UPPERCASE</code>, <code>MAX</code>, <code>MIN</code>, <code>SUBSTR</code>, <code>TRIM</code>, <code>TRANSLATE</code>, <code>VERIFY</code>, <code>SYSPARM</code>, <code>SYSTEM</code>, <code>SYSVERSION</code>, <code>COMPILEDATE</code>, <code>COMPILETIME</code>, <code>MACNAME</code>, <code>PARMSET</code>, and more</td></tr><tr><td>Compiler options</td><td><code>%PROCESS</code> directives: <code>MACRO(CASE(...))</code>, <code>MACRO(DBCS(...))</code>, <code>MACRO(FIXED(...))</code>, <code>MACRO(RESCAN(...))</code>, <code>LP(32|64)</code>, <code>SYSPARM(...)</code>, <code>CMPAT(...)</code></td></tr></tbody></table><h2 id="what-about-the-external-preprocessor" tabindex="-1"><a class="header-anchor" href="#what-about-the-external-preprocessor"><span>What about the external preprocessor?</span></a></h2><p>The external preprocessor option is not going away. It remains the right tool for one important use case: <strong>non-standard preprocessing</strong>. If your codebase uses custom macros or preprocessing syntax beyond what the PL/I compiler natively defines, the external preprocessor remains the mechanism that handles it.</p><p>However, if you use the external preprocessor <em>only</em> for standard PL/I macro expansion, we strongly encourage you to disable it. Running both means that you lose the richer editor experience described earlier. The result is flat coloring, the empty hovers on declarations, and no Go to Declaration support.</p><p><strong>If you use only standard PL/I macros:</strong> Disable the external preprocessor in your project settings and let the native support take over. This approach provides improved coloring, live hovers, and navigation.</p><p><strong>If you use non-standard preprocessing:</strong> Check whether the preprocessor can be configured to skip standard macro processing and handle only the custom parts. If it can, you get the best of both worlds: native editor support for standard macros and external preprocessing support for custom syntax.</p><h2 id="availability" tabindex="-1"><a class="header-anchor" href="#availability"><span>Availability</span></a></h2><p>Native PL/I macro preprocessor support is available in <strong>ZOpen Editor 6.7.0</strong> and will be available in <strong>IBM Developer for z/OS 18.0.0</strong>.</p><hr><p><em>What other PL/I editing features would make a difference to your daily work? Share your feedback in the comments.</em></p>`,28)])}var f=c(u,[[`render`,d]]);export{l as _pageData,f as default};