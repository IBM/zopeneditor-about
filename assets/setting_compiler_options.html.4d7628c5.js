import{r as s,o as r,a as c,b as e,d as n,F as i,c as t,e as a}from"./app.4dc079aa.js";import{_ as p}from"./plugin-vue_export-helper.21dcd24c.js";const l={},d=t('<h1 id="setting-compiler-options" tabindex="-1"><a class="header-anchor" href="#setting-compiler-options" aria-hidden="true">#</a> Setting compiler options</h1><p>Compiler options affect how the editor interprets your COBOL or PL/I programs for editing. You can specify compiler options in property groups in <a href="setting_propertygroup">settings</a> or <a href="zapp">ZAPP files</a> as well as inline in individual program files. The following sections provide references and examples for the options currently supported for each language by the editor.</p><h2 id="cobol" tabindex="-1"><a class="header-anchor" href="#cobol" aria-hidden="true">#</a> COBOL</h2><p><em>Table 1. COBOL Compiler Options</em></p><table><thead><tr><th style="text-align:center;">Aspect of your program</th><th>Compiler options</th><th>Default</th><th>Option abbreviations</th></tr></thead><tbody><tr><td style="text-align:center;">Source language</td><td>CURRENCY</td><td>NOCURRENCY</td><td>CURR|NOCURR</td></tr></tbody></table><h3 id="currency" tabindex="-1"><a class="header-anchor" href="#currency" aria-hidden="true">#</a> CURRENCY</h3><p>You can use the <code>CURRENCY</code> compiler option to provide an alternative default currency symbol to be used for COBOL program.</p><h4 id="syntax" tabindex="-1"><a class="header-anchor" href="#syntax" aria-hidden="true">#</a> Syntax</h4><p><code>&#39;CURRENCY= NO |literal</code></p><p>Default: <code>CURRENCY= $</code></p><p><code>CURRENCY=NO</code> is the default option. <code>CURRENCY</code> abbreviations are <code>CURR</code> or <code>NOCURR</code>.</p><p><code>literal</code> Represents the default currency symbol that you want to use in your program. The literal must be an alphanumeric literal (optionally a hexadecimal literal) representing a 1-byte EBCDIC character that must not be any of the following items:</p>',12),u=t("<li>Digits zero (0) through nine (9)</li><li>Uppercase alphabetic characters: <code>A B C D P R</code> <code>S V X Z</code></li><li>Lowercase alphabetic characters a through z</li><li>The space</li><li>Special characters: <code>\\* + - / , . ; ( ) = &quot;</code></li><li>Uppercase alphabetic character G, if the COBOL program defines a DBCS item with the <code>PICTURE</code> symbol G. The <code>PICTURE</code> clause will not be valid for that <code>DBCS</code> item because the symbol G is considered to be a currency symbol in the <code>PICTURE</code> clause.</li><li>Uppercase alphabetic character N, if the COBOL program defines a DBCS item with the <code>PICTURE</code> symbol N. The <code>PICTURE</code> clause will not be valid for that DBCS item because the symbol N is considered to be a currency symbol in the <code>PICTURE</code> clause.</li>",7),h=a("Uppercase alphabetic character E, if the COBOL program defines an external floating-point item. The "),m=e("code",null,"PICTURE",-1),b=a(" clause will not be valid for the external floating-point item because the symbol E is considered to be a currency symbol in the "),g=e("code",null,"PICTURE",-1),f=a(" clause. For more details, see "),y={href:"https://www.ibm.com/support/knowledgecenter/en/SS6SG3_6.3.0/custom/igycch214.html",target:"_blank",rel:"noopener noreferrer"},k=a("CURRENCY compiler option"),x=t(`<h4 id="examples" tabindex="-1"><a class="header-anchor" href="#examples" aria-hidden="true">#</a> Examples</h4><p>You can define the <code>CURRENCY</code> option in the source program as a suboption or in a property group compiler option.</p><p>To define in the source program, use the following lines:</p><div class="language-COBOL ext-COBOL line-numbers-mode"><pre class="language-COBOL"><code>cbl CURRENCY(\u20AC);\`
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>To define in property groups, add the following lines:</p><div class="language-json ext-json line-numbers-mode"><pre class="language-json"><code> <span class="token property">&quot;zopeneditor.propertygroups&quot;</span><span class="token operator">:</span> <span class="token punctuation">[</span>
<span class="token punctuation">{</span>
      <span class="token property">&quot;name&quot;</span><span class="token operator">:</span> <span class="token string">&quot;Sample&quot;</span><span class="token punctuation">,</span>
      <span class="token property">&quot;compilerOptions&quot;</span><span class="token operator">:</span> <span class="token string">&quot;currency(\u20AC)&quot;</span><span class="token punctuation">,</span>
      <span class="token property">&quot;language&quot;</span><span class="token operator">:</span> <span class="token string">&quot;cobol&quot;</span>
    <span class="token punctuation">}</span>
 <span class="token punctuation">]</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>After you properly defined the currency option in property groups or in the source program as a suboption, you can use <code>\u20AC</code> in your source program. For example:</p><pre><code>  01 WS-TEST-FIELD            PIC \u20AC\u20AC\u20AC,\u20AC\u20AC9.99.
</code></pre>`,8),R=a("For more details, see "),O={href:"https://www.ibm.com/support/knowledgecenter/en/SS6SG3_6.3.0/custom/igycch214.html",target:"_blank",rel:"noopener noreferrer"},v=a("CURRENCY compiler options"),N=t(`<h2 id="pl-i" tabindex="-1"><a class="header-anchor" href="#pl-i" aria-hidden="true">#</a> PL/I</h2><p><em>Table 1. PL/I Compiler Options</em></p><table><thead><tr><th style="text-align:center;">Compile-time option</th><th>Abbreviated</th><th>z/OS\xAE default</th></tr></thead><tbody><tr><td style="text-align:center;">MARGINS(m,n[,c]) | NOMARGINS</td><td>MAR(m,n)</td><td>MARGINS F-format: (2,72) V-format: (10,100)</td></tr><tr><td style="text-align:center;">NOT</td><td>-</td><td>NOT(&#39;\xAC&#39;)</td></tr><tr><td style="text-align:center;">OR(&#39;c&#39;)</td><td>-</td><td>OR(&#39; | &#39;)</td></tr></tbody></table><h3 id="margins" tabindex="-1"><a class="header-anchor" href="#margins" aria-hidden="true">#</a> MARGINS</h3><p>The <code>MARGINS</code> option specifies which part of each compiler input record contains PL/I statements, and the position of the ANS control character that formats the listing, if the <code>SOURCE</code> option, the <code>INSOURCE</code> option, or both apply. The compiler does not process data that is outside these limits, but it does include it in the source listings. The PL/I source is extracted from the source input records so that the first data byte of a record immediately follows the last data byte of the previous record. For variable records, you must ensure that when you need a blank, you explicitly insert it between margins of the records.</p><p>Syntax:</p><div class="language-txt ext-txt line-numbers-mode"><pre class="language-txt"><code>MARGINS(--+-m-+--,--+-n--+--+------+--)-.| &#39;-,--c-&#39; |
&gt; &gt; -+-NOMARGINS----------------------+---------------&gt;&lt;
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><h4 id="m" tabindex="-1"><a class="header-anchor" href="#m" aria-hidden="true">#</a> <code>m</code></h4><p>The column number of the leftmost character (first data byte) that is processed by the compiler. It must not exceed 100.</p><h4 id="n" tabindex="-1"><a class="header-anchor" href="#n" aria-hidden="true">#</a> <code>n</code></h4><p>The column number of the rightmost character (last data byte) that is processed by the compiler. It should be greater than <code>m</code>, but must not exceed 200. Variable-length records are effectively padded with blanks to give them the maximum record length.</p><h4 id="c" tabindex="-1"><a class="header-anchor" href="#c" aria-hidden="true">#</a> <code>c</code></h4><p>The column number of the ANS printer control character. It must not exceed 200, and it should be outside the values specified for <code>m</code> and <code>n</code>. A value of 0 for <code>c</code> indicates that no ANS control character is present. Only the following control characters can be used:</p><p><code>(blank): skip one line before printing</code></p><p><code>0: skip two lines before printing</code></p><p><code>\u2013: skip three lines before printing</code></p><p><code>+: no skip before printing</code></p><p><code>1: start new page</code></p><p>Any other character is an error and is replaced by a blank.</p><p>Do not use a value of <code>c</code> that is greater than the maximum length of a source record, because this causes the format of the listing to be unpredictable. To avoid this problem, put the carriage control characters to the left of the source margins for variable-length records.</p><p>Specifying <code>MARGINS(,,c)</code> is an alternative to using <code>%PAGE</code> and <code>%SKIP</code> statements according to the PL/I Language Reference.</p><p>The IBM\xAE-supplied default for fixed-length records is <code>MARGINS(2,72)</code>. For variable-length and undefined-length records, the IBM-supplied default is <code>MARGINS(10,100)</code>. This specifies that there is no printer control character.</p><p>Use the <code>MARGINS</code> option to override the default for the primary input in a program. The secondary input must have the same margins as the primary input.</p><p>The <code>NOMARGINS</code> option will suppress any previously encountered instance of the <code>MARGINS</code> option. The purpose of this option is to allow your installation to have a default set of compile-time options that use a <code>MARGINS</code> option tailored for their fixed format source preferences while retaining the ability to use variable source format files. You would usually specify the <code>NOMARGINS</code> option, if you use it at all, as part of the parameter-string passed to the compiler. The compiler will ignore <code>NOMARGINS</code> if it finds the option in a <code>%PROCESS</code> statement.</p><h4 id="examples-1" tabindex="-1"><a class="header-anchor" href="#examples-1" aria-hidden="true">#</a> Examples</h4><p>Define the <code>MARGINS</code> compiler option either in the source program or in property groups.</p><p>To define in the source program, use the following lines:</p><div class="language-PL ext-PL line-numbers-mode"><pre class="language-PL"><code>%process MARGINS(&lt;column number of the leftmost character, column number of the rightmost character&gt;);
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>To define in property groups, add the following lines:</p><div class="language-json ext-json line-numbers-mode"><pre class="language-json"><code> <span class="token property">&quot;zopeneditor.propertygroups&quot;</span><span class="token operator">:</span> <span class="token punctuation">[</span>
   <span class="token punctuation">{</span>
      <span class="token property">&quot;name&quot;</span><span class="token operator">:</span> <span class="token string">&quot;Sample&quot;</span><span class="token punctuation">,</span>
      <span class="token property">&quot;compilerOptions&quot;</span><span class="token operator">:</span> <span class="token string">&quot;MARGINS(2,100)&quot;</span><span class="token punctuation">,</span>
      <span class="token property">&quot;language&quot;</span><span class="token operator">:</span> <span class="token string">&quot;pl1&quot;</span>
    <span class="token punctuation">}</span>
 <span class="token punctuation">]</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><h3 id="not" tabindex="-1"><a class="header-anchor" href="#not" aria-hidden="true">#</a> NOT</h3><p>You can use the <code>NOT</code> option to specify up to seven alternative symbols that can be used as the logical <code>NOT</code> operator.</p><h4 id="syntax-1" tabindex="-1"><a class="header-anchor" href="#syntax-1" aria-hidden="true">#</a> Syntax</h4><p><code>NOT(&#39;---char-+-&#39;)</code></p><p>char:</p><p>A single <code>SBCS</code> character</p><p>You cannot specify any of the alphabetic characters, digits, or special characters defined in the PL/I Language Reference, except for the standard logical <code>NOT</code> symbol (\xAC). You must specify at least one valid character. When you specify the <code>NOT</code> option, the standard <code>NOT</code> symbol is no longer recognized unless you specify it as one of the characters in the character string. For example, <code>NOT(&#39;~&#39;)</code> means that the tilde character, <code>&#39;A1&#39;X</code>, will be recognized as the logical <code>NOT</code> operator, and the standard NOT symbol, <code>&#39;\xAC&#39;</code>, <code>&#39;5F&#39;X</code>, will not be recognized. Similarly, <code>NOT(&#39;~\xAC&#39;)</code> means that either the tilde or the standard <code>NOT</code> symbol will be recognized as the logical <code>NOT</code> operator. The IBM-supplied default code point for the <code>NOT</code> symbol is <code>&#39;5F&#39;X</code>. The logical <code>NOT</code> sign might appear as a logical NOT symbol <code>(\xAC)</code> or a caret symbol <code>(^)</code> on your keyboard.</p><h4 id="default" tabindex="-1"><a class="header-anchor" href="#default" aria-hidden="true">#</a> Default</h4><p><code>NOT(\xAC)</code></p><h4 id="examples-2" tabindex="-1"><a class="header-anchor" href="#examples-2" aria-hidden="true">#</a> Examples</h4><p>Define <code>NOT</code> compiler option either in the source program or in property groups.</p><p>To define in the source program, use the following lines:</p><div class="language-PL ext-PL line-numbers-mode"><pre class="language-PL"><code>%process NOT(symbol);
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>To define in property groups, add the following lines:</p><div class="language-json ext-json line-numbers-mode"><pre class="language-json"><code> <span class="token property">&quot;zopeneditor.propertygroups&quot;</span><span class="token operator">:</span> <span class="token punctuation">[</span>
   <span class="token punctuation">{</span>
      <span class="token property">&quot;name&quot;</span><span class="token operator">:</span> <span class="token string">&quot;Sample&quot;</span><span class="token punctuation">,</span>
      <span class="token property">&quot;compilerOptions&quot;</span><span class="token operator">:</span> <span class="token string">&quot;NOT(!)&quot;</span><span class="token punctuation">,</span>
      <span class="token property">&quot;language&quot;</span><span class="token operator">:</span> <span class="token string">&quot;pl1&quot;</span>
    <span class="token punctuation">}</span>
 <span class="token punctuation">]</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>After proper definition, you are ready to use your custom <code>NOT</code> symbol in your program. For example:</p><div class="language-PL ext-PL line-numbers-mode"><pre class="language-PL"><code>    if (! abc &gt; 0 )
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><h3 id="or" tabindex="-1"><a class="header-anchor" href="#or" aria-hidden="true">#</a> OR</h3><p>The <code>OR</code> option specifies up to seven alternative symbols as the logical <code>OR</code> operator. These symbols are also used as the concatenation operator, which is defined as two consecutive logical <code>OR</code> symbols.</p><h4 id="syntax-2" tabindex="-1"><a class="header-anchor" href="#syntax-2" aria-hidden="true">#</a> Syntax</h4><p><code>OR(&#39;---char-+-&#39;)</code></p><p>char:</p><p>A single <code>SBCS</code> character</p><p>You cannot specify any of the alphabetic characters, digits, and special characters defined in the PL/I Language Reference, except for the standard logical <code>OR</code> symbol <code>(|)</code>. You must specify at least one valid character. If you specify the <code>OR</code> option, the standard <code>OR</code> symbol is no longer recognized unless you specify it as one of the characters in the character string. For example, <code>OR(&#39;\\&#39;)</code> means that the backslash character, <code>&#39;E0&#39;X</code>, will be recognized as the logical <code>OR</code> operator, and two consecutive backslashes will be recognized as the concatenation operator. The standard <code>OR</code> symbol, <code>&#39;|&#39;</code>, <code>&#39;4F&#39;X</code>, will not be recognized as either operator. Similarly, <code>OR(&#39;\\|&#39;)</code> means that either the backslash or the standard <code>OR</code> symbol will be recognized as the logical <code>OR</code> operator, and either symbol or both symbols can be used to form the concatenation operator.</p><p>PL/I parser will parse your source code up to the last column number you specify in your margins setting.</p><h4 id="default-1" tabindex="-1"><a class="header-anchor" href="#default-1" aria-hidden="true">#</a> Default</h4><p><code>(|)</code></p><h4 id="examples-3" tabindex="-1"><a class="header-anchor" href="#examples-3" aria-hidden="true">#</a> Examples</h4><p>Define <code>OR</code> compiler option either in the source program or in property groups.</p><p>To define in the source program, use the following lines:</p><div class="language-PL ext-PL line-numbers-mode"><pre class="language-PL"><code>%process OR(symbol);
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>To define in property groups, add the following lines:</p><div class="language-json ext-json line-numbers-mode"><pre class="language-json"><code> <span class="token property">&quot;zopeneditor.propertygroups&quot;</span><span class="token operator">:</span> <span class="token punctuation">[</span>
   <span class="token punctuation">{</span>
      <span class="token property">&quot;name&quot;</span><span class="token operator">:</span> <span class="token string">&quot;Sample&quot;</span><span class="token punctuation">,</span>
      <span class="token property">&quot;compilerOptions&quot;</span><span class="token operator">:</span> <span class="token string">&quot;OR(|)&quot;</span><span class="token punctuation">,</span>
      <span class="token property">&quot;language&quot;</span><span class="token operator">:</span> <span class="token string">&quot;pl1&quot;</span>
    <span class="token punctuation">}</span>
 <span class="token punctuation">]</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>After proper definition, you are ready to use your custom <code>OR</code> symbol in your program. For example:</p><div class="language-PL ext-PL line-numbers-mode"><pre class="language-PL"><code>    if (xyz | yz)
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><h2 id="references" tabindex="-1"><a class="header-anchor" href="#references" aria-hidden="true">#</a> References</h2>`,66),T={href:"https://www.ibm.com/support/knowledgecenter/en/SS6SG3_6.3.0/custom/igycch200.html",target:"_blank",rel:"noopener noreferrer"},C=a("Enterprise COBOL compiler options"),S={href:"https://www.ibm.com/support/knowledgecenter/SSY2V3_5.2.0/com.ibm.ent.pl1.zos.doc/pg/optns.html",target:"_blank",rel:"noopener noreferrer"},w=a("PL/I compile-time option descriptions");function _(q,I){const o=s("ExternalLinkIcon");return r(),c(i,null,[d,e("ul",null,[u,e("li",null,[h,m,b,g,f,e("a",y,[k,n(o)])])]),x,e("p",null,[R,e("a",O,[v,n(o)])]),N,e("ul",null,[e("li",null,[e("a",T,[C,n(o)])]),e("li",null,[e("a",S,[w,n(o)])])])],64)}var E=p(l,[["render",_]]);export{E as default};
