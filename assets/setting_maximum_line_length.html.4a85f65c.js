import{r as s,o as i,a as o,b as n,d as r,w as l,F as m,c as t,e}from"./app.72ab0fa2.js";import{_ as d}from"./plugin-vue_export-helper.21dcd24c.js";const c={},u=t(`<h1 id="setting-language-specific-maximum-line-length" tabindex="-1"><a class="header-anchor" href="#setting-language-specific-maximum-line-length" aria-hidden="true">#</a> Setting language-specific maximum line length</h1><p>You can set the maximum line length for COBOL, PL/I, HLASM, REXX, and JCL program files with IBM Z Open Editor so that a file that exceeds the maximum line length is not allowed to be saved.</p><p>If a file that exceeds the maximum line length is being saved, the following warning is displayed:</p><div class="language-json ext-json line-numbers-mode"><pre class="language-json"><code>Truncation warning<span class="token operator">:</span> You are trying to save a file that exceeds the maximum line length. You can configure the line length in Preferences. Saving the file will truncate all text after column <span class="token number">80</span>. Do you want to continue?
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>If you choose <code>Yes</code>, all text after column 80 or a column number that was specified in the preference settings will be removed by the editor.</p><p>If you choose <code>No</code>, the file will not be saved. To continue saving the file, you can either change the maximum line length or remove the text after the column number that is specified in the preference settings.</p>`,6),h=e("To configure the maximum line length, add "),p=n("code",null,"zopeneditor.language.maximumLineLength",-1),g=e(", such as "),x=n("code",null,"zopeneditor.cobol.maximumLineLength",-1),b=e(", in settings.json, and provide a 0-based column number to your user or workspace settings. For information about adding user or workspace settings, see "),f=e("Accessing settings for IBM Z Open Editor"),L=e("."),v=t(`<h2 id="setting-the-cobol-maximum-line-length" tabindex="-1"><a class="header-anchor" href="#setting-the-cobol-maximum-line-length" aria-hidden="true">#</a> Setting the COBOL maximum line length</h2><p>To configure the COBOL maximum line length, add <code>zopeneditor.cobol.maximumLineLength</code> in <code>settings.json</code> and provide 0-based column number to your user or workspace settings:</p><div class="language-json ext-json line-numbers-mode"><pre class="language-json"><code><span class="token property">&quot;zopeneditor.cobol.maximumLineLength&quot;</span><span class="token operator">:</span> <span class="token number">80</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The default maximum line length setting for COBOL is <code>80</code>.</p><h2 id="setting-the-pl-i-maximum-line-length" tabindex="-1"><a class="header-anchor" href="#setting-the-pl-i-maximum-line-length" aria-hidden="true">#</a> Setting the PL/I maximum line length</h2><p>To configure the PL/I maximum line length, add <code>zopeneditor.pl1.maximumLineLength</code> in <code>settings.json</code> and provide 0-based column number to your user or workspace settings:</p><div class="language-json ext-json line-numbers-mode"><pre class="language-json"><code><span class="token property">&quot;zopeneditor.pl1.maximumLineLength&quot;</span><span class="token operator">:</span> <span class="token number">80</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The default maximum line length setting for PL/I is <code>80</code>.</p><h2 id="setting-the-hlasm-maximum-line-length" tabindex="-1"><a class="header-anchor" href="#setting-the-hlasm-maximum-line-length" aria-hidden="true">#</a> Setting the HLASM maximum line length</h2><p>To configure the HLASM maximum line length, add <code>zopeneditor.hlasm.maximumLineLength</code> in <code>settings.json</code> and provide 0-based column number to your user or workspace settings:</p><div class="language-json ext-json line-numbers-mode"><pre class="language-json"><code><span class="token property">&quot;zopeneditor.hlasm.maximumLineLength&quot;</span><span class="token operator">:</span> <span class="token number">80</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The default maximum line length setting for HLASM is <code>80</code>.</p><h2 id="setting-the-rexx-maximum-line-length" tabindex="-1"><a class="header-anchor" href="#setting-the-rexx-maximum-line-length" aria-hidden="true">#</a> Setting the REXX maximum line length</h2><p>To configure the REXX maximum line length, add <code>zopeneditor.rexx.maximumLineLength</code> in <code>settings.json</code> and provide 0-based column number to your user or workspace settings:</p><div class="language-json ext-json line-numbers-mode"><pre class="language-json"><code><span class="token property">&quot;zopeneditor.rexx.maximumLineLength&quot;</span><span class="token operator">:</span> <span class="token number">80</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The default maximum line length setting for REXX is <code>80</code>.</p><h2 id="setting-the-jcl-maximum-line-length" tabindex="-1"><a class="header-anchor" href="#setting-the-jcl-maximum-line-length" aria-hidden="true">#</a> Setting the JCL maximum line length</h2><p>To configure the JCL maximum line length, add <code>zopeneditor.jcl.maximumLineLength</code> in <code>settings.json</code> and provide 0-based column number to your user or workspace settings:</p><div class="language-json ext-json line-numbers-mode"><pre class="language-json"><code><span class="token property">&quot;zopeneditor.jcl.maximumLineLength&quot;</span><span class="token operator">:</span> <span class="token number">80</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The default maximum line length setting for JCL is <code>80</code>.</p>`,20);function _(j,k){const a=s("RouterLink");return i(),o(m,null,[u,n("p",null,[h,p,g,x,b,r(a,{to:"/Docs/setting_preferences.html"},{default:l(()=>[f]),_:1}),L]),v],64)}var T=d(c,[["render",_]]);export{T as default};
