import{r,o as l,a as c,b as a,d as s,w as p,F as i,e,c as n}from"./app.087f62ff.js";import{_ as u}from"./plugin-vue_export-helper.21dcd24c.js";const d={},h=a("h1",{id:"cobol-pl-i-preprocessor-tutorial",tabindex:"-1"},[a("a",{class:"header-anchor",href:"#cobol-pl-i-preprocessor-tutorial","aria-hidden":"true"},"#"),e(" COBOL, PL/I Preprocessor tutorial")],-1),m=e("The Git repository at "),k={href:"https://github.com/ibm/zopeneditor-sample/tree/wazi-main/preprocessor",target:"_blank",rel:"noopener noreferrer"},b=e("https://github.com/ibm/zopeneditor-sample/tree/wazi-main/preprocessor"),g=e(" contains example configuration and program files for trying the COBOL and PL/I Preprocessor support available in Z Open Editor. It contains examples for custom preprocessors written in Java as well as REXX that you can easily build and execute."),f=n('<p>Once you completed this tutorial you should be able to write and use your own preprocessor in a similar way by replacing the command line and parameters in the configuration files shown here.</p><p>To be able to use the Z Open Editor Preprocessor feature and these examples you must have imported a valid trial or license key in the Welcome page that activate the feature.</p><h2 id="review-the-zapp-file-and-create-zapp-user-or-workspace-variables" tabindex="-1"><a class="header-anchor" href="#review-the-zapp-file-and-create-zapp-user-or-workspace-variables" aria-hidden="true">#</a> Review the ZAPP file and create ZAPP user or workspace variables</h2><ol><li><p>Open the samples <a href="../zapp.yaml">ZAPP file</a> and find the <code>local-cobol</code> or <code>local-pli</code> preprocessor profile defined there. It references three variables for executing the preprocessor: <code>${JAVA_HOME}</code>, <code>${WORKSPACE}</code>, and <code>${HLQ}</code> (used for the remote preprocessor examples only). You find placeholders for these three in this <a href="../.vscode/settings.json">samples workspace settings file</a>.</p></li><li><p>Open that file and either</p><ol><li>replace the three values with your absolute <code>$JAVA_HOME</code> path and the absolute path of this workspace, i.e. where you cloned this Git repository on your development machine, or</li><li>move these variables to your user settings as these are specific to your local setup, or</li><li>just replace the variables in the ZAPP profiles shown below with their concrete values.</li></ol></li></ol><h2 id="local-preprocessor-examples" tabindex="-1"><a class="header-anchor" href="#local-preprocessor-examples" aria-hidden="true">#</a> Local preprocessor examples</h2><h3 id="prepare-the-sample-preprocessor-program" tabindex="-1"><a class="header-anchor" href="#prepare-the-sample-preprocessor-program" aria-hidden="true">#</a> Prepare the sample preprocessor program</h3>',6),y=e("To use the sample preprocessor you need to build it using Java. The folder "),P=e("preprocessor/<COBOL|PLI>/local-preprocessor"),w=e(" contains a Java Maven project that you can use to build it by running "),v=a("code",null,"mvn package",-1),O=e(" from that folder."),L=e("Alteratively, use the "),x={href:"https://marketplace.visualstudio.com/items?itemName=vscjava.vscode-java-pack",target:"_blank",rel:"noopener noreferrer"},I=e("Extension Pack for Java"),C=e(" to build it from VS Code (Java and Maven required in your path)."),E=n(`<p>Local COBOL profile sample :</p><div class="language-yaml ext-yml line-numbers-mode"><pre class="language-yaml"><code>    <span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> local<span class="token punctuation">-</span>cobol
        <span class="token key atrule">language</span><span class="token punctuation">:</span> cobol
        <span class="token key atrule">type</span><span class="token punctuation">:</span> preprocessor
        <span class="token key atrule">location</span><span class="token punctuation">:</span> local
        <span class="token key atrule">settings</span><span class="token punctuation">:</span>
          <span class="token key atrule">command</span><span class="token punctuation">:</span> $<span class="token punctuation">{</span>JAVA_HOME<span class="token punctuation">}</span>/bin/java <span class="token punctuation">-</span>jar $<span class="token punctuation">{</span>WORKSPACE<span class="token punctuation">}</span>/preprocessor/COBOL/local<span class="token punctuation">-</span>preprocessor/target/local<span class="token punctuation">-</span>cobol<span class="token punctuation">-</span>preprocessor<span class="token punctuation">-</span>1.0<span class="token punctuation">-</span>SNAPSHOT.jar <span class="token punctuation">{</span>input_file<span class="token punctuation">}</span> $<span class="token punctuation">{</span>output_file<span class="token punctuation">}</span>
          <span class="token key atrule">outputPath</span><span class="token punctuation">:</span> ./preprocessor/output
          <span class="token key atrule">fileExtension</span><span class="token punctuation">:</span> cee
          <span class="token key atrule">environmentVariables</span><span class="token punctuation">:</span>
            <span class="token key atrule">some</span><span class="token punctuation">:</span> var
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>Local PL1 profile sample :</p><div class="language-yaml ext-yml line-numbers-mode"><pre class="language-yaml"><code>    <span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> local<span class="token punctuation">-</span>pli
        <span class="token key atrule">language</span><span class="token punctuation">:</span> pl1
        <span class="token key atrule">type</span><span class="token punctuation">:</span> preprocessor
        <span class="token key atrule">location</span><span class="token punctuation">:</span> local
        <span class="token key atrule">settings</span><span class="token punctuation">:</span>
          <span class="token key atrule">command</span><span class="token punctuation">:</span> $<span class="token punctuation">{</span>JAVA_HOME<span class="token punctuation">}</span>/bin/java <span class="token punctuation">-</span>jar $<span class="token punctuation">{</span>WORKSPACE<span class="token punctuation">}</span>/preprocessor/PLI/local<span class="token punctuation">-</span>preprocessor/target/local<span class="token punctuation">-</span>pl1<span class="token punctuation">-</span>preprocessor<span class="token punctuation">-</span>1.0<span class="token punctuation">-</span>SNAPSHOT.jar $<span class="token punctuation">{</span>input_file<span class="token punctuation">}</span> $<span class="token punctuation">{</span>output_file<span class="token punctuation">}</span>
          <span class="token key atrule">outputPath</span><span class="token punctuation">:</span> ./preprocessor/output
          <span class="token key atrule">fileExtension</span><span class="token punctuation">:</span> pci
          <span class="token key atrule">environmentVariables</span><span class="token punctuation">:</span>
            <span class="token key atrule">some</span><span class="token punctuation">:</span> var
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>Note, that if you are using Windows and have a space in the path (e.g. <code>Program Files</code>), you will need to replace the portion of the path that has spaces with the Windows shorthand. Generally, this will be the first 6 non-whitespace characters in the path followed by <code>~1</code>, for example, for <code>Program Files</code> this would be <code>Progra~1</code>, for a directory like <code>test files</code> this would be <code>testfi~1</code>. If the path is shorter than 6 non-whitespace characters, you will need to use <code>Cmd</code> to get the shorthand. To do this, open <code>Cmd</code>, go to the directory above the directory you need the shorthand for, e.g. <code>C:/</code> for <code>C:/Program Files</code>. Run <code>dir /x</code>, and the directories will be listed with their shorthands.</p>`,5),R=e("Also note the location of the "),S=a("code",null,"outputPath",-1),_=e(" setting in the profile pointing to the directory "),A=e("preprocessor/output"),D=e(". This is the folder were the preprocessed programs will be generated in."),T=n(`<h3 id="running-the-local-preprocessor-from-z-open-editor" tabindex="-1"><a class="header-anchor" href="#running-the-local-preprocessor-from-z-open-editor" aria-hidden="true">#</a> Running the local preprocessor from Z Open Editor</h3><p>Now you are ready to run the preprocessor on our sample programs.</p><ol><li><p>Open either the file <a href="./COBOL/PrintApp.cbl">preprocessor/COBOL/PrintApp.cbl</a> or <a href="./PLI/PrintApp.pli">preprocessor/PLI/PrintApp.cbl</a>. You will see that Z Open Editor will show many syntax errors as it contains macros such as <code>+ID</code>, <code>+DD</code>, etc. The sample preprocessor will replace them with valid COBOL or Pl/I code behind the covers to enable our language to parse them.</p></li><li><p>Right-click inside the editor and select &quot;Execute local preprocessor command&quot;.</p></li><li><p>A progress bar will open and as a result all the syntax errors will go away. If something goes wrong check the log output for error messages in the VS Code Output view under the Z Open Editor drop-down value.</p></li><li><p>You can now hover over the preprocessor macros and see the code that has been used to replace them in a hover. For example the <code>+ID.</code> COBOL statement will show in the hover that it was replaced with <code>IDENTIFICATION DIVISION.</code>. The PL/I <code>+DL</code> statement will show a replacement with <code>DECLARE</code>.</p></li><li><p>Open the generated output file <a href="./output/PrintApp.cee">preprocessor/output/PrintApp.cee</a> in the editor. You see the file generated by the preprocessor, which is a valid COBOL or PL/I program. The Z Open Editor language server will use that program file to parse for errors and overlay the results to the original programs with macros in the editor.</p></li><li><p>Go back to <a href="./COBOL/PrintApp.cbl">preprocessor/COBOL/PrintApp.cbl</a> or <a href="./PLI/PrintApp.pli">preprocessor/PLI/PrintApp.cbl</a> and select &quot;Compare preprocessor input and output files&quot; from the context menu. It will of a Diff editor showing you the two files and their differences side by side.</p></li><li><p>Open you user settings and find <code>zopeneditor.autoPreprocessor</code>. From the drop-down select <code>all</code>. Switch back to the editor and make changes to the sample program. You might see syntax errors if you use the macros in your statements. However, when you save the program you can see that the preprocessor is execute automatically and the errors related to the macros will go away. Examine the other drop-down values for when the preprocessor should be triggered.</p></li></ol><h2 id="remote-preprocessor-example" tabindex="-1"><a class="header-anchor" href="#remote-preprocessor-example" aria-hidden="true">#</a> Remote preprocessor example</h2><h3 id="review-the-zapp-file-and-prepare-your-data-sets" tabindex="-1"><a class="header-anchor" href="#review-the-zapp-file-and-prepare-your-data-sets" aria-hidden="true">#</a> Review the ZAPP file and prepare your data sets</h3><p>To run the remote preprocessor example that performs on z/OS we assume you have created a Zowe Explorer profile for RSE API or z/OSMF and made it your default using the <code>zopeneditor.zowe&quot;: {&quot;profile-name&quot;}</code> user setting. The profile name should be displayed in the status bar of the editor. Note, that for z/OSMF you must also define a TSO Zowe CLI profile as our REXX preprocessor example will be executed using TSO. For RSE API profiles this is not required as RSE API has a TSO interface built-in.</p><ol><li><p>Open the samples <a href="../zapp.yaml">ZAPP file</a> and find the <code>remote-cobol</code> or <code>remote-pli</code> preprocessor profile defined there. It references several data set names and the <code>\${HLQ}</code> variable.</p></li><li><p>Either replace the variable with your high-level qualifier or define a ZAPP variable in your user or workspace settings as described above.</p></li></ol><p>Remote COBOL profile sample :</p><div class="language-yaml ext-yml line-numbers-mode"><pre class="language-yaml"><code><span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> remote<span class="token punctuation">-</span>cobol
  <span class="token key atrule">language</span><span class="token punctuation">:</span> cobol
  <span class="token key atrule">type</span><span class="token punctuation">:</span> preprocessor
  <span class="token key atrule">location</span><span class="token punctuation">:</span> mvs
  <span class="token key atrule">settings</span><span class="token punctuation">:</span>
    <span class="token key atrule">commandDataSet</span><span class="token punctuation">:</span> $<span class="token punctuation">{</span>HLQ<span class="token punctuation">}</span>.PREPROC(CBLPRPC)
    <span class="token key atrule">tempDataHLQ</span><span class="token punctuation">:</span> $<span class="token punctuation">{</span>HLQ<span class="token punctuation">}</span>
    <span class="token key atrule">outputDataSet</span><span class="token punctuation">:</span> $<span class="token punctuation">{</span>HLQ<span class="token punctuation">}</span>.PREPROC.OUTPUT()
    <span class="token key atrule">commandParameters</span><span class="token punctuation">:</span> <span class="token string">&quot;&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>Remote PL1 profile sample :</p><div class="language-yaml ext-yml line-numbers-mode"><pre class="language-yaml"><code><span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> remote<span class="token punctuation">-</span>pli
  <span class="token key atrule">language</span><span class="token punctuation">:</span> pl1
  <span class="token key atrule">type</span><span class="token punctuation">:</span> preprocessor
  <span class="token key atrule">location</span><span class="token punctuation">:</span> mvs
  <span class="token key atrule">settings</span><span class="token punctuation">:</span>
    <span class="token key atrule">commandDataSet</span><span class="token punctuation">:</span> $<span class="token punctuation">{</span>HLQ<span class="token punctuation">}</span>.PREPROC(PLIPRPC)
    <span class="token key atrule">tempDataHLQ</span><span class="token punctuation">:</span> $<span class="token punctuation">{</span>HLQ<span class="token punctuation">}</span>
    <span class="token key atrule">outputDataSet</span><span class="token punctuation">:</span> $<span class="token punctuation">{</span>HLQ<span class="token punctuation">}</span>.PREPROC.OUTPUT()
    <span class="token key atrule">commandParameters</span><span class="token punctuation">:</span> <span class="token string">&quot;&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><ol start="3"><li><p>Create a PDSE for <code>commandDataSet</code> and <code>outputDataSet</code>. You can use the suggested names from the ZAPP or create them using other names and update the ZAPP profile with these names.</p></li><li><p>You also need to create or reuse an existing PDSE for the COBOL sample program <a href="./COBOL/PrintApp.cbl">preprocessor/COBOL/PrintApp.cbl</a>, such as <code>\${HLQ}.PREPROC.COBOL</code> and the equivalent for PL/I. It needs to have the words <code>COBOL</code> or <code>CBL</code> (<code>PLI</code> or <code>PL1</code>) in its name so Z Open Editor will use its default file associations to open the file with the COBOL language server. The sample preprocessor will also create a temporal sequential data set using your HLQ and delete it after completion.</p></li><li><p>Use Zowe Explorer to upload the REXX sample preprocessor program <a href="./COBOL/remote-preprocessor/CBLPRPC.rexx">preprocessor/remote-preprocessor/CBLPRPC.rexx</a> to the <code>commandDataSet</code> you created in the previous step.</p></li><li><p>Upload the <a href="./COBOL/PrintApp.cbl">preprocessor/COBOL/PrintApp.cbl</a> to the PDSE you have created for it in the previous step, such as <code>\${HLQ}.PREPROC.COBOL</code>. For Pl/I use the equivalent files.</p></li></ol><h3 id="running-the-remote-preprocessor-from-z-open-editor" tabindex="-1"><a class="header-anchor" href="#running-the-remote-preprocessor-from-z-open-editor" aria-hidden="true">#</a> Running the remote preprocessor from Z Open Editor</h3><ol><li><p>Open the <code>PRINTAPP</code> program via Zowe Explorer and ensure that it was recognized as a COBOL or PL/I program showing you syntax highlighting, an outline view, etc. If your PDSE does not contain the keywords required then you can click the status bar in VS Code to select <code>COBOL</code> as the language to use for this file. The sample program was not processed, yet, so it will show syntax errors.</p></li><li><p>Right-click inside the editor and select &quot;Execute remote preprocessor command&quot;. A progress bar will open and as a result all the syntax errors will go away. If something goes wrong check the log output for error messages in the VS Code Output view under the Z Open Editor drop-down value.</p></li><li><p>You can now hover over the preprocessor macros and see the code that has been used to replace them in a hover. For example the <code>+ID.</code> statement will show in the hover that it was replaced with <code>IDENTIFICATION DIVISION.</code>.</p></li><li><p>Open the generated output file that was created in the <code>outputDataSet</code> in the editor. You see the file generated by the preprocessor, which is a valid COBOL program. The Z Open Editor language server will use that program file to parse for errors and overlay the results to the original programs with macros in the editor.</p></li><li><p>Open you user settings and find <code>zopeneditor.autoPreprocessor</code>. From the drop-down select <code>all</code>. Switch back to the editor and make changes to the sample program. You might see syntax errors if you use the macros in your statements. However, when you save the program you can see that the preprocessor is executed automatically and the errors related to the macros will go away. Examine the other drop-down values for when the preprocessor should be triggered.</p></li></ol><h2 id="remote-preprocessor-for-local-files-example" tabindex="-1"><a class="header-anchor" href="#remote-preprocessor-for-local-files-example" aria-hidden="true">#</a> Remote preprocessor for local files example</h2><h3 id="review-the-remote-zapp-profile-and-prepare-your-data-sets-with-programdataset" tabindex="-1"><a class="header-anchor" href="#review-the-remote-zapp-profile-and-prepare-your-data-sets-with-programdataset" aria-hidden="true">#</a> Review the remote ZAPP profile and prepare your data sets with programDataSet</h3><p>You can also run a remote preprocessor on files that you edit locally, e.g. as part of your Git repository. For that Z Open Editor will upload the program file you are currently editing to a dedicated PDSE, execute the preprocessor on z/OS and then fetch the output file back to the local file system.</p><ol><li><p>To run the remote preprocessor example on local files, ensure that the <a href="#remote-preprocessor-example">Remote preprocessor example</a> works as expected.</p></li><li><p>Review the ZAPP file and find the profiles <code>remote-cobol</code> or <code>remote-pli</code>.</p></li><li><p>Comment out the last line with the <code>programDataSet</code> property, which will be location where the local file will be uploaded.</p></li><li><p>Create such a PDSE and provide the value for this property. You can use the suggested names from the ZAPP file or create them using other names and update the ZAPP profile with these names.</p></li></ol><p>Remote COBOL profile sample:</p><div class="language-yaml ext-yml line-numbers-mode"><pre class="language-yaml"><code><span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> remote<span class="token punctuation">-</span>cobol
  <span class="token key atrule">language</span><span class="token punctuation">:</span> cobol
  <span class="token key atrule">type</span><span class="token punctuation">:</span> preprocessor
  <span class="token key atrule">location</span><span class="token punctuation">:</span> mvs
  <span class="token key atrule">settings</span><span class="token punctuation">:</span>
    <span class="token key atrule">commandDataSet</span><span class="token punctuation">:</span> $<span class="token punctuation">{</span>HLQ<span class="token punctuation">}</span>.PREPROC(CBLPRPC)
    <span class="token key atrule">tempDataHLQ</span><span class="token punctuation">:</span> $<span class="token punctuation">{</span>HLQ<span class="token punctuation">}</span>
    <span class="token key atrule">outputDataSet</span><span class="token punctuation">:</span> $<span class="token punctuation">{</span>HLQ<span class="token punctuation">}</span>.PREPROC.OUTPUT()
    <span class="token key atrule">commandParameters</span><span class="token punctuation">:</span> <span class="token string">&quot;&quot;</span>
    <span class="token key atrule">programDataSet</span><span class="token punctuation">:</span> $<span class="token punctuation">{</span>HLQ<span class="token punctuation">}</span>.PREPROC.COBOL()
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>Remote PL1 profile sample:</p><div class="language-yaml ext-yml line-numbers-mode"><pre class="language-yaml"><code><span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> remote<span class="token punctuation">-</span>pli
  <span class="token key atrule">language</span><span class="token punctuation">:</span> pl1
  <span class="token key atrule">type</span><span class="token punctuation">:</span> preprocessor
  <span class="token key atrule">location</span><span class="token punctuation">:</span> mvs
  <span class="token key atrule">settings</span><span class="token punctuation">:</span>
    <span class="token key atrule">commandDataSet</span><span class="token punctuation">:</span> $<span class="token punctuation">{</span>HLQ<span class="token punctuation">}</span>.PREPROC(PLIPRPC)
    <span class="token key atrule">tempDataHLQ</span><span class="token punctuation">:</span> $<span class="token punctuation">{</span>HLQ<span class="token punctuation">}</span>
    <span class="token key atrule">outputDataSet</span><span class="token punctuation">:</span> $<span class="token punctuation">{</span>HLQ<span class="token punctuation">}</span>.PREPROC.OUTPUT()
    <span class="token key atrule">commandParameters</span><span class="token punctuation">:</span> <span class="token string">&quot;&quot;</span>
    <span class="token key atrule">programDataSet</span><span class="token punctuation">:</span> $<span class="token punctuation">{</span>HLQ<span class="token punctuation">}</span>.PREPROC.COBOL()
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><h3 id="running-the-remote-preprocessor-for-local-files-from-z-open-editor" tabindex="-1"><a class="header-anchor" href="#running-the-remote-preprocessor-for-local-files-from-z-open-editor" aria-hidden="true">#</a> Running the remote preprocessor for local files from Z Open Editor</h3><ol><li><p>Open either the file <a href="./COBOL/PrintApp.cbl">preprocessor/COBOL/PrintApp.cbl</a> or <a href="./PLI/PrintApp.pli">preprocessor/PLI/PrintApp.cbl</a>. You will see that Z Open Editor will show many syntax errors as it contains macros such as <code>+ID</code>, <code>+DD</code>, etc. The sample preprocessor will replace them with valid COBOL or Pl/I code behind the covers to enable our language to parse them.</p></li><li><p>Right-click inside the editor and select &quot;Execute remote preprocessor command for local file&quot;.</p></li><li><p>A progress bar will open and as a result all the syntax errors will go away. You can now hover over the preprocessor macros and see the code that has been used to replace them in a hover. For example the <code>+ID.</code> statement will show in the hover that it was replaced with <code>IDENTIFICATION DIVISION.</code>.</p></li><li><p>Open the generated output file that was created in the <code>outputDataSet</code> in the editor. You see the file generated by the preprocessor, which is a valid COBOL program. The Z Open Editor language server will use that program file to parse for errors and overlay the results to the original programs with macros in the editor.</p></li></ol><h2 id="expanding-pl-i-macros-using-a-remote-preprocessor" tabindex="-1"><a class="header-anchor" href="#expanding-pl-i-macros-using-a-remote-preprocessor" aria-hidden="true">#</a> Expanding PL/I macros using a remote preprocessor</h2><p>To expand the PL/I macros using remote preprocessor, we assume that your z/OS has a PL/I compiler and you have created a Zowe Explorer profile for RSE API or z/OSMF and made it your default using the <code>zopeneditor.zowe&quot;: {&quot;profile-name&quot;}</code> user setting.</p><ol><li><p>Open the samples <a href="../zapp.yaml">ZAPP file</a> and find the <code>remote-pli-macro</code> preprocessor profile defined there. As the first preprocessor profile gets picked up on the basis of language and location make sure whichever profile you want to run is ordered at the top under the preprocessor section or you can comment out the other preprocessor profiles.</p></li><li><p>It references several data set names and a <code>HLQ</code> variable. Either replace the variable with your high-level qualifier or define a ZAPP variable in your user or workspace settings.</p></li><li><p>Also comment out the PL/I property groups that were defined for the PSAM example as these do not work with the preprocessor example.</p></li></ol><div class="language-yaml ext-yml line-numbers-mode"><pre class="language-yaml"><code><span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> remote<span class="token punctuation">-</span>pli<span class="token punctuation">-</span>macro
  <span class="token key atrule">language</span><span class="token punctuation">:</span> pl1
  <span class="token key atrule">type</span><span class="token punctuation">:</span> preprocessor
  <span class="token key atrule">location</span><span class="token punctuation">:</span> mvs
  <span class="token key atrule">settings</span><span class="token punctuation">:</span>
    <span class="token key atrule">commandDataSet</span><span class="token punctuation">:</span> $<span class="token punctuation">{</span>HLQ<span class="token punctuation">}</span>.SFELSAMP(FEKRNPLI)
    <span class="token key atrule">tempDataHLQ</span><span class="token punctuation">:</span> $<span class="token punctuation">{</span>HLQ<span class="token punctuation">}</span>
    <span class="token key atrule">outputDataSet</span><span class="token punctuation">:</span> $<span class="token punctuation">{</span>HLQ<span class="token punctuation">}</span>.PREPROC.OUTPUT()
    <span class="token key atrule">commandParameters</span><span class="token punctuation">:</span> &lt;SYSPRINT<span class="token punctuation">&gt;</span>$<span class="token punctuation">{</span>HLQ<span class="token punctuation">}</span>.LOG.OUTPUT()&lt;/SYSPRINT<span class="token punctuation">&gt;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><ol start="4"><li><p>Create a PDSE for <code>commandDataSet</code> with PL/I macro preprocessor <a href="./PLI/remote-preprocessor/FEKRNPLI.rexx">preprocessor/PLI/remote-preprocessor/FEKRNPLI.rexx</a> as a member in it, and <code>outputDataSet</code>. You can use the suggested names from the ZAPP or create them using other names and update the ZAPP profile with these names.</p></li><li><p>To append <code>&lt;SYSPRINT&gt;</code> tag in the XML use <code>commandParameters</code> in the ZAPP profile. For creating the <code>SYSPRINT</code> dataset <code>\${HLQ}.LOG.OUTPUT()</code> use following dataset attributes:</p></li></ol><div class="language-ascii ext-ascii line-numbers-mode"><pre class="language-ascii"><code>    Record Format (recfm): VBA
    Record Length (lrecl): 137
    Dataset Type (dsntp): LIBRARY
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><ol start="6"><li><p>In <code>FEKRNPLI.rexx</code> you need to replace the <code>&quot;.V6R1M0.SIBMZCMP(IBMZPLI)&quot;</code> from <code>compiler = compiler_hlq||&quot;.V6R1M0.SIBMZCMP(IBMZPLI)&quot;</code> in line <code>82</code> with the PL/I compiler in your current z/OS system.</p></li><li><p>You also need to create or reuse an existing PDSE for the PLI sample program <a href="../PLI/CITYCODE.pli">PLI/CITYCODE.pli</a>, such as <code>\${HLQ}.PREPROC.PLI</code>. It needs to have the words <code>PLI</code> or <code>PL1</code> in its name so Z Open Editor will use its default file associations to open the file with the PL/I language server. The sample preprocessor will also create a temporal sequential data set using your HLQ and delete it after completion.</p></li><li><p>Open the <code>CITYCODE</code> via Zowe Explorer and ensure that it was recognized as a PL/I program showing you syntax highlighting, an outline view, etc. If your PDSE does not contain the keywords required then you can click the status bar in VS Code to select <code>PLI</code> as the language to use for this file. The sample program was not processed, yet, so it will show syntax errors.</p></li><li><p>Right-click inside the editor and select &quot;Execute remote preprocessor command&quot;.</p></li><li><p>A progress bar will open and as a result all the syntax errors will go away. You can now hover over the preprocessor macros and see the code that has been used to replace them in a hover. For example the macro statements including <code>%</code> will be resolved and removed. For example in the <code>CITYCODE.pli</code> file from line <code>14</code> to <code>16</code> which are macros will be removed. Similarly other macro statements will get resolved.</p></li></ol><div class="language-ascii ext-ascii line-numbers-mode"><pre class="language-ascii"><code> %DCL USE CHAR;
 %USE = &#39;FUN&#39; /* FOR SUBROUTINE, %USE = &#39;SUB&#39; */ ;
 %IF USE = &#39;FUN&#39; %THEN %DO;
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><strong>Note:</strong> Using this sample will still result in a RC=12 from the PL/I compiler even though it does produce a valid output file. This is expected and still means this preprocessor has finished successfully.</p>`,33);function B(Z,H){const t=r("ExternalLinkIcon"),o=r("RouterLink");return l(),c(i,null,[h,a("p",null,[m,a("a",k,[b,s(t)]),g]),f,a("ol",null,[a("li",null,[y,s(o,{to:"/Docs/local-preprocessor/"},{default:p(()=>[P]),_:1}),w,v,O]),a("li",null,[L,a("a",x,[I,s(t)]),C])]),E,a("p",null,[R,S,_,s(o,{to:"/Docs/output/"},{default:p(()=>[A]),_:1}),D]),T],64)}var $=u(d,[["render",B]]);export{$ as default};
